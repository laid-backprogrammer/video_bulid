import React from 'react';
import ReactDOM from 'react-dom/client';
import AgentStudioApp from './agent-studio/AgentStudioApp';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <AgentStudioApp />
  </React.StrictMode>
);
